import { ConvertPermissionPipe } from './convert-permission.pipe';

describe('ConvertPermissionPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPermissionPipe();
    expect(pipe).toBeTruthy();
  });
});
